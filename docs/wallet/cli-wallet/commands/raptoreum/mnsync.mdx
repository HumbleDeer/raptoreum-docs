---
sidebar_position: 6
---
import ReactPlayer from 'react-player'

# mnsync

*mnsync status|next|reset*

Returns the sync status, updates to the next step or resets it entirely.

`raptoreum-cli mnsync status`

<ReactPlayer playing controls url={[
    {src: `${require('./assets/placeholder.webm').default}`, type: 'video/webm'}]} />


## Output

| Result | Type | Description |
|--|--|--|
| ... | ... | ... |

```bash
Result:
{
  ...
}
```

## Examples

```bash
curl --user myusername --data-binary '{"jsonrpc": "1.0", "id":"curltest", "method": "mnsync", "params": ["status"] }' -H 'content-type: text/plain;' http://127.0.0.1:9998/
```


