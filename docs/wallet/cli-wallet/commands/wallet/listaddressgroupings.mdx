---
sidebar_position: 31
---
import ReactPlayer from 'react-player'

# listaddressgroupings

*listaddressgroupings*

Lists groups of addresses which have had their common ownership
made public by common use as inputs or as the resulting change
in past transactions

`raptoreum-cli listaddressgroupings `

<ReactPlayer playing controls url={[
    {src: `${require('./assets/listaddressgroupings.webm').default}`, type: 'video/webm'}]} />

## Output

| Result | Type | Description |
|--|--|--|
| "address" | (string) | The raptoreum address |

```bash
Result:
[
  [
    [
      "address",            (string) The raptoreum address
      amount,                 (numeric) The amount in RTM
      "account"             (string, optional) DEPRECATED. The account
    ]
    ,...
  ]
  ,...
]
```

## Examples

```bash
curl --user myusername --data-binary '{"jsonrpc": "1.0", "id":"curltest", "method": "listaddressgroupings", "params": [] }' -H 'content-type: text/plain;' http://127.0.0.1:9998/
```

