---
sidebar_position: 3
---
import ReactPlayer from 'react-player'

# addmultisigaddress

*addmultisigaddress nrequired ["key",...] ( "account" )*

Add a nrequired-to-sign multisignature address to the wallet. Requires a new wallet backup.
Each key is a Raptoreum address or hex-encoded public key.
If 'account' is specified (DEPRECATED), assign address to that account.

```bash
# Add a multisig address from 2 addresses
raptoreum-cli addmultisigaddress 2 "[\"Xt4qk9uKvQYAonVGSZNXqxeDmtjaEWgfrS\",\"XoSoWQkpgLpppPoyyzbUFh1fq2RBvW6UK2\"]"
```

<ReactPlayer playing controls url={[
    {src: `${require('./assets/addmultisigaddress.webm').default}`, type: 'video/webm'}]} />


## Arguments

| Argument | Type | Description |
|--|--|--|
| nrequired | (number) | The number of required signatures out of the n keys or  |
| "keys" | (string) | A json array of raptoreum addresses or hex-encoded public keys  |
| "account" | (string) | DEPRECATED. An account to assign the addresses to.  |

```bash
Arguments:
1. nrequired        (numeric, required) The number of required signatures out of the n keys or addresses.
2. "keys"         (string, required) A json array of raptoreum addresses or hex-encoded public keys
     [
       "address"  (string) raptoreum address or hex-encoded public key
       ...,
     ]
3. "account"      (string, optional) DEPRECATED. An account to assign the addresses to.
```

## Output

| Result | Type | Description |
|--|--|--|
| "address"| (string) | A raptoreum address associated with the keys. |

```bash

Result:
"address"         (string) A raptoreum address associated with the keys.
```


## Examples

```bash
curl --user myusername --data-binary '{"jsonrpc": "1.0", "id":"curltest", "method": "addmultisigaddress", "params": [2, "[\"Xt4qk9uKvQYAonVGSZNXqxeDmtjaEWgfrS\",\"XoSoWQkpgLpppPoyyzbUFh1fq2RBvW6UK2\"]"] }' -H 'content-type: text/plain;' http://127.0.0.1:9998/
```

